query TorrentLibraryMetrics($input: TorrentLibraryMetricsInput!) {
  torrent {
    libraryMetrics(input: $input) {
      snapshots {
        bucket
        totalCount
        totalSize
        classifiedCount
      }
    }
    contentBreakdown {
      contentType
      count
    }
  }
}
