query TorrentLibraryMetrics($input: TorrentLibraryMetricsInput!) {
  torrent {
    libraryMetrics(input: $input) {
      snapshots {
        bucket
        totalCount
        totalSize
      }
    }
    contentBreakdown {
      contentType
      count
    }
  }
}
